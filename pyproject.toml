[project]
name = "allygo-ai-worker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lch2023", email = "lch2023@naver.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
allygo-ai-worker = "allygo_ai_worker.worker:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = true